
const HomePage = ({user}) => {


    return (
        <div>
            <h2>{`${user.username} loggedin`}</h2>
            <h1>Medium by Rabin</h1>

            
        </div>
    )
};

export default HomePage;